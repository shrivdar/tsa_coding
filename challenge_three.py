import numpy as np

def Main():
  int total = 0
  int average = 0
  dataset = input("Please enter the dataset: ")
  my_dict = {}
  for i in range(0, len(string), 3):
    total += int(string[i])
    my_dict[string[i]] += 1

  average = total/i
  print("Average of the dataset: " + average)

  for value in mydict.values():
    print(my_dict[value] + " appeared " + value + " times within the dataset")
